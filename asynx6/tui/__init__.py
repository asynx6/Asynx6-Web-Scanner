"""Optional Textual TUI for interactive scanning.

Importing this package requires `pip install textual`. The package is a thin
wrapper; Asynx6App.run() raises ImportError with an install hint if the
dependency is missing.
"""